# Lab-clock-react

A simple dynamic color clock built with React and Vite.
This app displays the current date and time using the date-fns library and updates every second.
It features a clean, colorful design with a green background and a beige clock face.